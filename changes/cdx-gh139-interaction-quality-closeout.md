Category: changed
Audience: developers, users
Breaking-Change: no
Summary: Finish the gh-139 interaction work. Fix hit-test tolerance so it tracks zoom, add a rectangle query that handles cell borders, and wire snapping that respects the Alt key and drag hysteresis. Add a thread-affinity check for blocking services, make preflight start correctly, and fix the trace counters. Update the scheduler and contract docs.

