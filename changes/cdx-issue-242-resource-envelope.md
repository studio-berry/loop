Category: fixed
Audience: developers
Breaking-Change: no
Summary: Fix eight resource-envelope correctness issues: use shared_ptr-backed reservation for the active document model so destruction order cannot dangle; add exception safety to PDFDocumentContext::replaceDocument() so a budget-exceeded failure on session construction leaves the previous document intact; reserve the loaded document model in PdfTool so the envelope reflects real resident bytes; capture the in-flight resource reservation in the page-surface render lambda so bytes stay reserved for the full duration of the render; reorder insertIntoCache() to evict before tryReserve so freed bytes are visible at admission; remove the setLimit() call in shedPrefetchAndQuality() that retroactively lowered the pool limit below a prior high-water mark; use catalog->getMemoryConsumptionEstimate() in estimateDocumentModelBytes() for accurate catalog sizing; and track Prefetch-priority sheds separately in PDFResourceBudget so prefetch_shed in the workload envelope is not falsely set by Visible or Background failures.
