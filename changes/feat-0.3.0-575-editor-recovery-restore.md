Category: internal
Audience: developers
Breaking-Change: no
Summary: Restore the value-level editor-recovery surface deleted with the Widgets libraries (issue #575) into LoopLibCore as pdf::RecoveryPolicy/clampRecoveryPolicy, pdf::RecoverySourceIdentity with inspectRecoverySource/classifyRecoverySource, and the source-status and candidate types those need; register the previously uncompiled UnitTests/tst_recoverytest.cpp as the new UnitTestsRecovery target (LoopLibCore + Qt6::Core + Qt6::Test, QTEST_APPLESS_MAIN, no Gui/Widgets) in UnitTests/CMakeLists.txt and map it to the core module in agent-policy.json; regenerate docs/generated/architecture-catalog.json (88 test targets) and docs/generated/phase5-widgets-inventory.json (79 targets, the new target carries no Widgets linkage); and update docs/EDITOR_RECOVERY.md to state what now exists (the value-level policy and identity surface plus its running suite) and what does not yet (the manager, the per-session QLockFile claim, the checkpoint write and generation rotation, the retention sweep, and the Editor wiring). The two RecoveryTest slots keep every assertion and bound they had; only the namespace qualifier and, for the clamp slot, the object under test move from a manager instance to the pure clamp function.
