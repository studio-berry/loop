Category: internal
Audience: developers
Breaking-Change: no
Summary: Add the interaction-performance trace corpus for issue #146: a scenario schema, a report schema, three seed scenarios with a digest manifest, and a CI checker that validates the corpus with no build and enforces that a failing run names the contract it broke and the phase responsible. Missing telemetry must be reported as unavailable rather than as zero.
