Category: internal
Audience: developers
Breaking-Change: no
Summary: Add .cursor/environment.json so Cloud Agents bootstrap the Linux dev environment via the existing scripts/setup-dev-env.sh (apt deps, vcpkg, Qt 6.11.1, CMake/Ninja configure).
