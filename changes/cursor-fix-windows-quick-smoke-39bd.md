Category: fixed
Audience: maintainers
Breaking-Change: no
Summary: Fix Windows relocated LoopEditor --quick-smoke access violations during MSI packaging by resolving packaged Qt plugin discovery without adding usr/bin to libraryPaths, deferring PDFDocumentSession creation and PDFDocumentContext parenting until DocumentViewSession construction finishes, initializing CommandCatalog and PDFDocumentContext before PDFJobScheduler using copy-free InitStage ordering, starting PDFJobScheduler workers on first submit, deferring PageSurfaceCoordinator snapshot rebuild and page-cache budget refresh until primeInitialSnapshot while routing PDFPageCacheBudget mutations through PDFDocumentContext and avoiding shared budget reads from LoopLibInteraction during attach, moving PDFPageCacheBudget mutex-backed methods into LoopLibCore, staging qoffscreen beside LoopEditor.exe and appending Plugins=plugins to qt.conf when windeployqt omits them, shipping LoopEditor as a console binary under LOOP_LOOP_DISTRIBUTION for headless smoke, running software Quick before native in relocated Windows smoke, skipping primaryScreen() DPI probing on Windows during startup, and failing Windows_MSI Build project steps on cmake/verify LASTEXITCODE.
