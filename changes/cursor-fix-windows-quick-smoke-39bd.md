Category: fixed
Audience: maintainers
Breaking-Change: no
Summary: Stop overriding windeployqt qt.conf plugin paths on Windows during LoopEditor --quick-smoke, skip Sentry for the smoke fast path, and emit staged stderr markers plus install-tree diagnostics when relocated Windows packaging smoke fails. Copy qoffscreen beside LoopEditor.exe, append Plugins=plugins to qt.conf, remove attachConsole, resolve Windows plugin search via packagedLibraryPaths instead of adding usr/bin to libraryPaths, initialize CommandCatalog and PDFDocumentContext before PDFJobScheduler worker threads using copy-free InitStage markers, defer empty-document PDFDocumentSession creation and parent PDFDocumentContext after DocumentViewSession member initialization to avoid Windows 0xC0000005 during relocated smoke, start PDFJobScheduler worker threads on first submit so Qt signal wiring during DocumentViewSession construction does not race background workers on Windows, defer PageSurfaceCoordinator initial snapshot rebuild and page-cache budget refresh until the view session shell is primed while avoiding eager getSession() during startup cache wiring, fail Windows_MSI Build project on cmake/verify LASTEXITCODE so a compile error cannot look like a missing loop-default.json, route --quick-smoke through the full parser/settings path, run software Quick backend before native on Windows, add finer stage logging through DocumentViewSession member initialization, EditorHost, and QML load, and avoid probing primaryScreen() DPI on Windows where headless packaging smoke can fault during EditorHost construction.
