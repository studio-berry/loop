Category: fixed
Audience: maintainers
Breaking-Change: no
Summary: Fix Windows relocated LoopEditor --quick-smoke access violations during MSI packaging by resolving packaged Qt plugin discovery without adding usr/bin to libraryPaths, deferring PDFDocumentSession creation and PDFDocumentContext parenting until DocumentViewSession construction finishes, seeding the shared page-cache total inside PDFDocumentContext construction so startup avoids cross-DLL PDFPageCacheBudget mutations, deriving PageSurfaceCoordinator surface-byte limits from projected cacheLimit/bounds instead of reading PDFPageCacheBudget across the LoopLibCore DLL boundary, exiting quick-smoke with std::_Exit(0) after scene-graph initialization to avoid Windows DLL teardown faults once startup is proven, installing Liberation fonts under usr/lib/fonts for packaged Qt text, initializing CommandCatalog and PDFDocumentContext before PDFJobScheduler using copy-free InitStage ordering, starting PDFJobScheduler workers on first submit, deferring PageSurfaceCoordinator snapshot rebuild and page-cache budget refresh until primeInitialSnapshot while routing runtime PDFPageCacheBudget mutations through PDFDocumentContext, attaching coordinators with non-owning page-cache budget pointers to avoid std::shared_ptr refcount faults across the LoopLibCore DLL boundary, moving PDFPageCacheBudget mutex-backed methods into LoopLibCore, staging qoffscreen beside LoopEditor.exe and appending Plugins=plugins to qt.conf when windeployqt omits them, shipping LoopEditor as a console binary under LOOP_LOOP_DISTRIBUTION for headless smoke, running software Quick before native in relocated Windows smoke, skipping primaryScreen() DPI probing on Windows during startup, and failing Windows_MSI Build project steps on cmake/verify LASTEXITCODE.
