Category: fixed
Audience: maintainers
Breaking-Change: no
Summary: Stop overriding windeployqt qt.conf plugin paths on Windows during LoopEditor --quick-smoke, skip Sentry for the smoke fast path, and emit staged stderr markers plus install-tree diagnostics when relocated Windows packaging smoke fails. Copy qoffscreen beside LoopEditor.exe, append Plugins=plugins to qt.conf, remove attachConsole, resolve Windows plugin search via packagedLibraryPaths instead of adding usr/bin to libraryPaths, route --quick-smoke through the full parser/settings path, run software Quick backend before native on Windows, add finer stage logging through DocumentViewSession member initialization, EditorHost, and QML load, and avoid probing primaryScreen() DPI on Windows where headless packaging smoke can fault during EditorHost construction.
