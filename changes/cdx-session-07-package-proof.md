Category: internal
Audience: Maintainers
Breaking-Change: no
Summary: Repair release-package qualification metadata, fail Windows packaging when Sentry debug-file upload errors instead of continuing on error, keep optional symbol publication no-oping only when SENTRY_AUTH_TOKEN is unset, and install gperf/vcpkg autotools build deps on all Linux CI paths that run vcpkg install.
