Category: added
Audience: developers, users
Breaking-Change: no
Summary: Extend EditorHost shell stress test to 1k-page production-scale document (gh-363) with Widgets-free Quick shell, verifying interaction revision fencing and overlay/surface demand remain fenced under load; add test-only DocumentViewSession accessor to EditorHost and optional LOUPE_STRESS_ENVELOPE workload envelope. Add an on-canvas overprint fidelity indicator (gh-49): the canvas now caches a per-page overprint flag on PDFPrecompiledPage, reports PDFRenderDiagnostics for the standard render path, and lets the operator escalate the current page to an authoritative PDFRenderPolicy::forOutputPreview() render and back without reopening the document; PDFInkCoverageProbe and PDFColorInventory now wire PDFRenderPolicy::forPreflightAnalysis() into the transparency renderer they already construct. Add a per-page grid-based PageSpatialIndex (gh-145) and wire EvidenceHitTestSource and FindingListHitTestSource to query it instead of scanning every candidate on every pointer move; HitTestDispatcher's precedence rules are unchanged. New UnitTestsHitTestSource covers empty/degenerate/overlapping/off-page geometry and large object counts. Fix gh-38 (F-04): PDFBleedFixup::apply() now charges its per-page edge-sample raster allocation against a pdf::PDFProcessingBudget instead of only bounding a single page via settings.maxRasterPixels, so a pathological multi-page bleed-fixup call has a bounded cumulative raster cost; update docs/BUG_HUNT_2026-08-04.md's finding crosswalk to correct the prior claim that this path already charged the shared budget (only PDFBleedMarginProbe did) and to record what remains open (an automated per-command cancellation regression test, and the Linux ASan/UBSan/fuzz session).
