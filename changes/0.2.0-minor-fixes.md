Category: added
Audience: developers, users
Breaking-Change: no
Summary: Extend EditorHost shell stress test to 1k-page production-scale document (gh-363) with Widgets-free Quick shell, verifying interaction revision fencing and overlay/surface demand remain fenced under load; add test-only DocumentViewSession accessor to EditorHost and optional LOUPE_STRESS_ENVELOPE workload envelope. Add an on-canvas overprint fidelity indicator (gh-49): the canvas now caches a per-page overprint flag on PDFPrecompiledPage, reports PDFRenderDiagnostics for the standard render path, and lets the operator escalate the current page to an authoritative PDFRenderPolicy::forOutputPreview() render and back without reopening the document; PDFInkCoverageProbe and PDFColorInventory now wire PDFRenderPolicy::forPreflightAnalysis() into the transparency renderer they already construct.
