# 0.1.2 S19b / #141 handoff

## Scope

S19b normalizes transient interaction state in the existing QWidget canvas.
`PDFInteractionState` is private to `LoupeLibWidgets`; it is not persisted,
serialized, or added to `PDFDocumentContext`, the scheduler contract, or the
Session 1 trace schema.

The state is fenced by the complete `PDFRevisionIdentity` and a local
interaction generation. Revision changes, cache-generation changes, Escape,
focus loss, invalid drag/drop, and widget teardown invalidate stale work.

## Delivered

- [x] Private state model and QWidget wiring.
- [x] Revision-conflict and cancellation tests.
- [x] Windows runtime dependency note for the new test binaries.
- [x] Static checks and focused CTest evidence.

## Integration evidence

- Integration branch: `cdx/0.1.2-s19-integration`.
- Integration base: `origin/0.1.2` at `c58e2679`.
- S19 commits applied: `f3334c81`, `121ebc17`, `5cc0f4ba`, and `c3255eb0`.
- Build prerequisite applied: `4310e07e`, which repairs the existing
  `PDFRepairOperation::impact` call sites required by the configured source
  tree; it is unrelated to the S19 behavior.
- Source integration tip before this handoff/changelog closeout: `136d78bf`.
- The final local closeout commit is the commit carrying this handoff; record its
  exact SHA with `git rev-parse HEAD`.
- Build directory: `build-s19-integration` (Visual Studio 17 2022, Release,
  Qt 6.11.1, `CMAKE_EXPORT_COMPILE_COMMANDS=ON`).
- Focused command:

  ```text
  QT_QPA_PLATFORM=offscreen ctest --test-dir build-s19-integration -C Release --output-on-failure -R "UnitTests(Accessibility|JobScheduler|InteractionTrace|InteractionState|InteractionWidget|DocumentSession)"
  ```

- Result: 6/6 tests passed, 0 failed, 0.83 seconds.
- `git diff --check` passed after formatting the touched C++ files.

The deployed Release test directory contains the Qt 6.11.1 runtime, offscreen
platform plugin, Loupe libraries, and vcpkg runtime DLLs. This proves the clean
deployed S19 test harness; it does not claim a full product installer or remote
CI result.

## Not delivered

- Qt Quick or hybrid-host work.
- A presentation-neutral interaction library.
- Persistent tile/content caching.
- A public or persisted interaction-state contract.

## Verification ledger

The final local closeout commit is the commit carrying this handoff; obtain its
exact SHA with `git rev-parse HEAD` after the closeout commit is created.

The deployed Release test directory contains the Qt 6.11.1 runtime and
plugins, `platforms/qoffscreen.dll`, `LoupeLibCore.dll`, `LoupeLibWidgets.dll`,
and the required vcpkg runtime DLLs. The directory is generated output and
remains untracked.

Focused CTest gate (Windows, `QT_QPA_PLATFORM=offscreen`):

```text
UnitTestsAccessibility ........... Passed
UnitTestsJobScheduler ............ Passed
UnitTestsInteractionTrace ........ Passed
UnitTestsInteractionState ........ Passed
UnitTestsInteractionWidget ....... Passed
UnitTestsDocumentSession ......... Passed
100% tests passed (6/6)
```

Aggregate exit-gate command:

```text
scripts/agent/check-change.py --base c58e2679aba02e8e8c13694dac0a57440e80a67b --build-dir build-s19-ninja
```

Result: `status=pass`. Source integrity, architecture catalog, policy
adapters, formatting, all five selected product/library/test targets, all 24
mapped test targets, clang-tidy, and the 24/24 focused CTest matrix passed.

The installed Windows clang-tidy 19.1.5 rejects the Qt 6.11.1 MSVC
`offsetof` macro in `QtCore/qplugin.h`. The final local static run used a
disposable `/FI` compatibility header in the untracked generated
`compile_commands.json`; no repository source, policy, or diagnostic was
suppressed. Remote CI and merged-SHA/release evidence remain pending because
this topic branch has not been pushed or merged.
