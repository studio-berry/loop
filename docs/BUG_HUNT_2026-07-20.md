# Bug hunt inventory — 2026-07-20

Branch: `Core-cli-engine` · commit `fed0e6a3` (+ follow-up fixes below)  
Scope: branch-local defects on MIC-309/319/#16 work plus inherited PDF4QT / Frisket surface.

---

## Sources

| Source | Finding | Action |
|--------|---------|--------|
| **Sentry** (`berry-studios` / `frisket`, EU) | Only unresolved issues are verification noise: [FRISKET-2](https://berry-studios.sentry.io/issues/FRISKET-2) (“It works!”), [FRISKET-1](https://berry-studios.sentry.io/issues/FRISKET-1) (`thread_demo`). No production crash backlog. | Resolve/ignore as intentional verify events |
| **Session / Pieces** | White-overprint test failed on inline `op`/`OP` fixture; PdfTool hung/crashed on Windows (`0xC0000409`) when `qoffscreen.dll` missing; corpus suite blocked by PdfTool subprocess | Fixed in `fed0e6a3` |
| **Linear / Notion** | MIC-309 (atomic batch), MIC-319 (white-overprint detection), MIC-320 (overprint render), MIC-304 (fuzz), Notion #2 (atomic writers), #7/#10/#11/#45/#47 (traversal) | See backlog table |
| **Static review** | Resume path-match, spill flush, atomic write gap, decoder bombs, Form-XObject font blind spot, non-atomic PdfTool writers, Sentry UTF-8 DB path, missing CodeQL / SECURITY | Fixed or filed |
| **Bugbot** (`fed0e6a3`) | (1) Auto-resume skips stale re-exports after workspace edit; (2) manifest `persistManifest` return ignored on success path | Fixed post-commit |

---

## Branch-local defects (fixed)

| ID | Symptom | Root cause | Fix | Tests |
|----|---------|------------|-----|-------|
| B1 | Resume skipped wrong outputs | Index-based manifest match | `findOutputIndexByPath` + `manifestCompatibleWithJob` | `resume_mismatchedManifestStartsFreshBatch` |
| B2 | Sidecar spill corrupt read | No `flush()` before `takeData()` | `preflightsidecarutils.h` flush on spill + read | `sidecarStreamBuffer_spillRoundTripsContent` |
| B3 | Atomic write deleted final on failure | Custom remove-then-rename | `QSaveFile` for manifest + `PDFDocumentWriter(safeWrite=true)` | PageMaster export tests |
| B4 | White-overprint test 0 warnings | Fixture used inline `op`/`OP`; engine reads ExtGState | Regenerated fixture via `/OP` `/op` in `gs` dict | `run_whiteOverprint_*` + negative `white-overprint-ok.pdf` |
| B5 | PdfTool crash / hang under ctest | `qoffscreen.dll` not beside exe when spawned | `POST_BUILD` copy + `setLibraryPaths` in `main.cpp` | Manual `PdfTool help`; corpus when built |
| B6 | Auto-resume after workspace edit | PageMaster auto-set `job.resume` when paths matched | Removed auto-resume in `mainwindow.cpp`; resume only when `job.resume` set explicitly | Existing resume unit tests (API-level) |
| B7 | Stale manifest after successful write | `persistManifest` return ignored on `written` path | Propagate failure into export result | — |

---

## Inherited defects (fixed this session)

| ID | Symptom | Fix | Linear / Notion |
|----|---------|-----|-----------------|
| I1 | PdfTool `unite`/`separate` truncated targets on crash | `writer.write(..., true)` | Notion #2 slice |
| I2 | Sentry DB path on non-ASCII Windows paths | `sentry_options_set_database_pathw` | — |
| I3 | Flate/LZW/RLE decompression bombs | 256× ratio cap, 256 MiB max, `/Columns` ≤ 16384 | MIC-304 residual → see backlog |
| I4 | Embedded-font check missed Form XObject fonts | Recursive `scanResources` + annotation AP streams | Partial — color/overprint in Forms still open |
| I5 | Overprint fill/stroke gating inconsistent | `PDFOverprintMode::appliesToContent()` | MIC-320 partial slice |
| I6 | `SECURITY.md` placeholder | Rewritten policy | Done |
| I7 | No CodeQL workflow | `.github/workflows/codeql.yml` | Done |

---

## Backlog (filed / updated in Linear)

| Priority | Topic | Linear | Notes |
|----------|-------|--------|-------|
| P1 | Overprint-correct rendering (full) | [MIC-320](https://linear.app/mbx2/issue/MIC-320) | Detection in MIC-319 done; `appliesToContent` slice landed; golden renderer parity still open |
| P1 | Preflight resource traversal (color / overprint in nested Forms) | [MIC-321](https://linear.app/mbx2/issue/MIC-321) | Font recursion done in `fed0e6a3`; extend to color-space + white-overprint paths inside Form XObjects |
| P0 residual | Decoder / fuzz coverage gaps after stream caps | [MIC-304](https://linear.app/mbx2/issue/MIC-304) comment | Flate/LZW/RLE capped; image/content-stream fuzz expansion still tracked on MIC-304 |
| P2 | CodeQL on `master` | [MIC-322](https://linear.app/mbx2/issue/MIC-322) | Workflow added in `fed0e6a3`; enable on first `master` merge |
| P2 | Bleed-stress / AI-art fixtures green in CI | [MIC-316](https://linear.app/mbx2/issue/MIC-316) | Corpus skips remain; stress fixtures post-V1 |
| — | PageMaster batch preflight gate | [MIC-312](https://linear.app/mbx2/issue/MIC-312) | Unrelated to this hunt; still Todo |
| — | veraPDF external checker | [MIC-147](https://linear.app/mbx2/issue/MIC-147) | Explicit non-goal |

### Completed Linear (this branch)

- [MIC-309](https://linear.app/mbx2/issue/MIC-309) — atomic per-output batch + manifest (Done)
- [MIC-319](https://linear.app/mbx2/issue/MIC-319) — white-overprint detection (Done)

---

## Verification matrix

```text
ctest -C Release -R "UnitTestsPageMasterExport|UnitTestsPreflightPlugin|UnitTestsPreflightEngine|UnitTestsOverprint" --output-on-failure
PdfTool.exe help
PdfTool.exe preflight <white-overprint.pdf> --profile <test-white-overprint.json>
```

Corpus (`UnitTestsPreflightCorpus`) requires PdfTool built and `qoffscreen` discoverable. **2026-07-20 run:** PdfTool healthy; corpus fails on pre-existing `color-rgb` fixture (`pass: true` vs expected `pass: false`) — color-mode check not firing on `color-rgb.pdf`; file under MIC-321 / color traversal follow-up.

---

## Explicit non-goals (this hunt)

- Full veraPDF / MIC-147
- Complete MIC-320 renderer work
- Expanding fuzz harnesses beyond MIC-304 tracking
- MCP_DOCKER / full Linux image rebuild
- Merge `Core-cli-engine` → `master`
