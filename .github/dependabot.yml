version: 2

updates:
  # Actions are referenced by full commit SHA, so dependabot-actions owns the
  # reviewable PRs that move an immutable pin to a newer release.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5